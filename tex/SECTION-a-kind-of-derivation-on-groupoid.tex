% !TEX root = main.tex
\section{A Kind of Derivation on Groupoid}\label{sec:a-kind-of-derivation-on-groupoid}
In \cref{definition:collatz-complement} we defined the Collatz complement, they follow the proposition \cref{proposition:collatz-complement-basic-prop2}.
Now let's forget those definitions before.
We will introduce a new kind of form to subsume those properties above. It's the extend of $(\sigma, \tau)$-derivation on groupoids.

Let $\mathbb{G}(+, G)$ be a groupoid and $\mathbb{R}(+, \cdot, R)$ be a ring.
With these, we can define groupoid homomorphisms and their derivtions from $\mathbb{G}$ to $\mathbb{R}$.
\begin{definition}[Cauchy homomorphism]
    Function $\mathcal{A}: \mathbb{G} \to \mathbb{R}$ is called a groupoid homomorphism, if $\forall a, b \in \mathbb{G}$ that $a + b \in \mathbb{G}$, there is $\mathcal{A}(a+b) = \mathcal{A}(a) \cdot \mathcal{A}(b)$.
\end{definition}
If $\forall b \in \mathbb{G}$, there is $\mathcal{A}(b) = 0$, we called that $\mathcal{A}$ is a zero groupoid homomorphism, and denote $\mathcal{A} = 0$.
Otherwise, we say $\mathcal{A} \ne 0$. And unless otherwise specified, all homomorphisms hereafter are supposed non-zero.
\begin{proposition}
    If $\mathcal{A} \ne 0$, then $\forall a \in \mathbb{G}$, there is $\mathcal{A}(a)\cdot\mathcal{A}(-a) = \mathcal{A}(0) = 1$. In other words, $\mathcal{A}(-a) = \mathcal{A}(a)^{-1}$. This requires the ring $\mathbb{R}$ at least is a division ring.
\end{proposition}
\begin{proposition}
    Denote $\mathrm{Cauchy}(\mathbb{G},\mathbb{R})$ as the set of all groupoid homomorphisms on $\mathbb{R}$, then $\mathrm{Cauchy}(\mathbb{G},\mathbb{R})$ is a group by
    \[ (\mathcal{A}\cdot\mathcal{B})(a) = \mathcal{A}(a)\cdot\mathcal{B}(a). \]
\end{proposition}
\begin{definition}[($\mathcal{A}$, $\mathcal{B}$)-derivation]~\label{def:quasi-derivation-on-groupoid}
    Given $\mathcal{A}, \mathcal{B}\in\mathrm{Cauchy}(\mathbb{G},\mathbb{R})$.
    There's another function $\mathfrak{F}: \mathbb{G} \to \mathbb{R}$ such that $\forall a, b \in \mathbb{G}$, if $a + b \in \mathbb{G}$, then
    \[ \mathfrak{F}(a+b) = \mathcal{A}(a)\cdot\mathfrak{B}(b) + \mathfrak{B}(a)\cdot\mathcal{B}(b) \]
    called the forward ($\mathcal{A}$, $\mathcal{B}$)-derivation.
    And function $\mathfrak{B}: \mathbb{G} \to \mathbb{R}$
    \[ \mathfrak{B}(a+b) = \mathcal{A}(b)\cdot\mathfrak{B}(a) + \mathfrak{B}(b)\cdot\mathcal{B}(a) \]
    called the backward ($\mathcal{A}$, $\mathcal{B}$)-derivation.
\end{definition}
Actually, ($\mathcal{A}$, $\mathcal{B}$)-derivation is the extension of the $(\sigma, \tau)$-derivation on groupoid.

Denote $\partial_{R}(\mathcal{A}, \mathcal{B})$ as the set of all right ($\mathcal{A}$, $\mathcal{B}$)-derivations and $\partial_{L}(\mathcal{A}, \mathcal{B})$ as the set of all left ($\mathcal{A}$, $\mathcal{B}$)-derivations.
If the left or right is not matter, i.e. $\mathbb{R}$ is commutative, we just denote them $\partial_{*}(\mathcal{A}, \mathcal{B})$.
Our discussion of ($\mathcal{A}$, $\mathcal{B}$)-derivations will base on $\mathbb{R}$ is commutative.
Given $\mathfrak{U} \in \partial_{*}(\mathcal{A}, \mathcal{B})$, we have
\begin{proposition}
    $\mathfrak{U}(0) = 0$.
\end{proposition}
\begin{proposition}
    $\mathcal{A}(a)\cdot\mathfrak{U}(-a) + \mathfrak{U}(a)\cdot\mathcal{B}(-a) = 0$.
\end{proposition}

\begin{proposition}
    $\partial_{*}(\mathcal{A}, \mathcal{B})$ is a $\mathbb{R}$-module by
    \[ (s\mathfrak{U} + t\mathfrak{V})(a) = s\mathfrak{U}(a) + t\mathfrak{V}(a). \]
\end{proposition}
Also, we denote $\mathfrak{U} = 0$ if it is a zero ($\mathcal{A}$, $\mathcal{B}$)-derivation.
And unless otherwise specified, all derivations hereafter are supposed non-zero.
However, we need another restriction of ($\mathcal{A}$, $\mathcal{B}$)-derivations to describe our problem.

We say $p \in\mathbb{G}$ is inseparable, if $\forall a, b \in\mathbb{G}-\{p, 0\}$, $p$ cannot be represented by $a + b$.
And we denote $\mathbb{P}(\mathbb{G}) = \{ p \in\mathbb{G} \mid p \text{ is inseparable}\}$. With this we have
\begin{definition}[kernel of ($\mathcal{A}$, $\mathcal{B}$)-derivation]
    Suppose $p \in \mathbb{P}(\mathbb{G})$. We say $\mathfrak{U}(p)$ is a kernel on $p$ of $\mathfrak{U}$.
\end{definition}
\begin{definition}
    We say a sequence $\mathbb{T}(a)$ on $\mathbb{G}$ is a partition of $a \in \mathbb{G}$, if $\forall c \in \mathbb{T}(a)$, $\exists b, d \in \mathbb{G}$, such that $a = b + c + d$ and $\sum_{c\in \mathbb{T}(a)}c = a$.  We call $c$ is a part of $a$, and denote $c\subset a$, denote $a\backslash c = b$ and $c/a = d$. If $\forall c \in \mathbb{T}(a)$, there is $c\in\mathbb{P}(\mathbb{G})$, then we also denote the partition as $\mathbb{P}(a)$.
\end{definition}
\begin{theorem}
    $\forall \mathfrak{U} \in \partial_{*}(\mathcal{A}, \mathcal{B})$, $\forall \mathbb{T}$, we have
    \[ \mathfrak{U}(a) = \sum_{c \in \mathbb{T}(a)}\mathcal{A}(a\backslash c)\cdot\mathfrak{U}(c)\cdot\mathcal{B}(c/a). \]
\end{theorem}
\begin{proof}
    Use induction. \newline
    $\forall b \in\mathbb{G}$, we have $\mathfrak{U}(c) = \mathcal{A}(c\backslash c)\cdot\mathfrak{U}(c)\cdot\mathcal{B}(c/c)$ because $\mathcal{A}(c\backslash c) = \mathcal{A}(0) = 1$ and $\mathcal{B}(c/c) = \mathcal{B}(0) = 1$.
    Suppose $\forall b \subset a$, it holds.
    And for $\cdot\backslash\cdot$ and $\cdot/\cdot$, they each construct a well-ordered sequences.
    Then for $a$, we have
    \begin{align*}
        \mathfrak{U}(a) = &\mathcal{A}(a \backslash c)\cdot\mathfrak{U}(c + c/a) + \mathfrak{U}(a \backslash c)\cdot\mathcal{B}(c+c/a) \\
            = &\mathcal{A}(a \backslash c + c)\cdot\mathfrak{U}(c/a) + \mathcal{A}(a \backslash c)\cdot\mathfrak{U}(c)\cdot\mathcal{B}(c/a) \\
            &+ \mathfrak{U}(a \backslash c)\cdot\mathcal{B}(c + c/a) \\
            = &\mathcal{A}(a \backslash c + c)\cdot\sum_{d \in \mathbb{T}(c/a)}\mathcal{A}((c/a)\backslash d)\cdot\mathfrak{U}(d)\cdot\mathcal{B}(d/(c/a)) \\
              &+ \mathcal{A}(a \backslash c)\cdot\mathfrak{U}(c)\cdot\mathcal{B}(c/a)  \\
              &+\sum_{e \in \mathbb{T}(a \backslash c)}\mathcal{A}((a\backslash c)\backslash e)\cdot\mathfrak{U}(e)\cdot\mathcal{B}(e/(a\backslash c))\cdot\mathcal{B}(c + c/a)
    \end{align*}
    Notice that \begin{align*}
        &a \backslash c + c + (c/a)\backslash d = a - (d + d/(c/a)) = a\backslash d,\\
        &e/(a\backslash c) + c + c/a = a - ((a\backslash c)\backslash e + e) = e/a,
    \end{align*}
    and
    \[   d/(c/a) = d/a,\quad (a\backslash c)\backslash e = a \backslash e, \]
    we have
    \begin{align*}
        \mathfrak{U}(a) = & \sum_{e \in \mathbb{T}(a \backslash c)}\mathcal{A}(a\backslash e)\cdot\mathfrak{U}(e)\cdot\mathcal{B}(e/a) + \mathcal{A}(a \backslash c)\cdot\mathfrak{U}(c)\cdot\mathcal{B}(c/a) \\
            &+\sum_{d \in \mathbb{T}(c/a)}\mathcal{A}(a\backslash d)\cdot\mathfrak{U}(d)\cdot\mathcal{B}(d/a)\\
            = &\sum_{c \in \mathbb{T}(a)}\mathcal{A}(a \backslash c)\cdot\mathfrak{U}(c)\cdot\mathcal{B}(c/a).
    \end{align*}
\end{proof}
Specially, For the kernel independent ($\mathcal{A}$, $\mathcal{B}$)-derivation, we have
\begin{corollary}
    If $\mathfrak{U} \in \partial(\mathcal{A}, \mathcal{B})$ that $\mathfrak{U}(p) = \gamma$, then
    \[ \mathfrak{U}(c) = \gamma \cdot\sum_{p \in \mathbb{P}(a)}\mathcal{A}(a\backslash p)\cdot\mathcal{B}(p/a). \]
\end{corollary}
This theorem also holds on those continuous system.
\begin{example}~\label{exa:continous-situation-with-exact-differential}
    All closed interval compose a groupoid
    \[G = \left\{[a, b] \text{ : $[a, b]$ is closed interval on real number field $R$}\right\}.\]
    We have $\mathcal{F}[a, b] = \exp{\int_{a}^{b}f(x)\rmd x}$ and $\mathcal{G}[a, b] = \exp{\int_{a}^{b}g(x)\rmd x}$ are both in $\mathrm{Cauchy}(G, R)$. Define
    \[ \mathfrak{U}[a, b] = \int_{a}^{b}\mathcal{F}[a, x]\cdot\mathcal{G}[x,b]\cdot(f(x) - g(x))\rmd x. \]
    We can verify that  $\mathfrak{U}\in \partial_{*}(\mathcal{F}, \mathcal{G})$. Its kernel is $f(x)\rmd x - g(x)\rmd x$.

    Now let $F(x) = \int_{a}^{x}f(x)\rmd x$ and $G(x) = - \int_{x}^{b}g(x)\rmd x$. We can derive that $F'(x) = f(x)$ and $G'(x) = g(x)$. With these, we have
    \begin{align}~\label{equ:better-kernel}
        \mathfrak{U}[a, b] &= \int_{a}^{b}\exp(F(x)-G(x))\cdot(f(x) - g(x))\rmd x \nonumber \\
        &= \int_{a}^{b}\exp(F(x)-G(x))\rmd(F(x) - G(x)) \nonumber \\
        &= \frac{\exp F(b)}{\exp G(b)} - \frac{\exp F(a)}{\exp G(a)} \nonumber\\
        &= \mathcal{F}[a, b] - \mathcal{G}[b, a]
    \end{align}
    which is also a difference of powers.
\end{example}
\begin{example}~\label{exa:continous-situation-with-kernel-independent}
    Also using the groupoid and functions. Define
    \[ \mathfrak{V}[a, b] = \int_{a}^{b}\mathcal{F}[a, x]\mathcal{G}[x,b]\rmd x. \]
    Then $\mathfrak{V} \in \partial(\mathcal{F}, \mathcal{G})$. Its kernel is exactly $\rmd x$. However,we can not get a simple result like \cref{equ:better-kernel}. But  from \cref{theorem:inductive-formula} we still speculate that it can be expressed as the difference of two powers with some coefficients.
\end{example}
\begin{definition}[varied derivation and varities of derivation]~\label{def:varied-derivation-and-varities-of-derivation}
Define
\begin{align*}
    \mathcal{C}\mathfrak{U} &= \sum_{c \in \mathbb{T}(a)}(\mathcal{C}\cdot\mathcal{A})(a \backslash c)\cdot\mathfrak{U}(c)\cdot\mathcal{B}(c/a),\\
    \mathfrak{U}\mathcal{C} &= \sum_{c \in \mathbb{T}(a)}\mathcal{A}(a \backslash c)\cdot\mathfrak{U}(c)\cdot(\mathcal{B}\cdot\mathcal{C})(c/a)
\end{align*}
are the $\sigma$-varied derivation and $\tau$-varied derivation. Define
\begin{align*}
    \delta_{\sigma}\mathfrak{U} &= \mathcal{C}\mathfrak{U} - \mathcal{C}(a)\cdot\mathfrak{U},\\
    \delta_{\tau}\mathfrak{U} &= \mathfrak{U}\mathcal{C} - \mathfrak{U}\cdot \mathcal{C}(a).
\end{align*}
are the $\sigma$-varities of the derivation and $\tau$-varities of the derivation.
\end{definition}
\begin{proposition}
    \begin{align*}
    \delta_{\sigma}\mathfrak{U} &= \sum_{c \in \mathbb{T}(a)}\left[1 - \mathcal{C}(c + c/a)\right]\cdot(\mathcal{C}\cdot\mathcal{A})(a \backslash c)\cdot\mathfrak{U}(c)\cdot\mathcal{B}(c/a),\\
    \delta_{\tau}\mathfrak{U} &= \sum_{c \in \mathbb{T}(a)}\mathcal{A}(a \backslash c)\cdot\mathfrak{U}(c)\cdot(\mathcal{B}\cdot\mathcal{C})(c/a)\cdot\left[1 - \mathcal{C}(a \backslash c + c)\right].
\end{align*}
\end{proposition}
\begin{proposition}
    Since $\mathcal{A}, \mathcal{B}$ and $\mathcal{C}$ are non-zero, and $\mathfrak{U}\ne 0$, we have $\delta_{\sigma}\mathfrak{U} \ne 0$, $\delta_{\tau}\mathfrak{U} \ne 0$. 
\end{proposition}

% In \cref{sec:collatz-source-value} and \cref{sec:distribution-of-source-value-set}, we deduce that the Collatz source values are hierarchical, and that there is an isomorphism between the source value sets of two numbers within the same hierarchical layer. Not only with that, 
% $\partial(\mathcal{A}, \mathcal{B})$ also separates $\mathfrak{U}(\mathbb{G})$ into distinct layers.

\begin{definition}[periodic mapping]
Given partition $\mathbb{P}(a) = \{a_i\}_{i\in I}$. We also denote $a_{k}^{j} = \sum_{j < i}^{i \le k}a_i$.
For mapping $F:\mathbb{G} \to \mathbb{R}$.
If $\exists t\in I$ such that $\forall a_i\in \mathbb{P}(a)$, there is $F(a_{i+t}) = F(a_i)$, then we say $F$ is $t$-periodic on $a$.
\end{definition}
\begin{definition}
    Denote all $t$-periodic Cauchy homomorphisms on $a$ as $\mathrm{O}_{t}(\mathbb{P}(a),\mathbb{R})$.
    And all derivations with $t$-periodic kernel denote as $\partial_{t}(\mathbb{P}(a),\mathbb{R})$.
\end{definition}
\begin{proposition}
    If $\mathcal{A} \in \mathrm{O}_{t}(\mathbb{P}(a), \mathbb{R})$, then $\mathcal{A}(a_{i+t}^{j+t}) = \mathcal{A}(a_{i}^{j})$.
\end{proposition}
\begin{proof}
    $\mathcal{A}(a_{i+t}^{j+t}) = \mathcal{A}(\sum_{j+t < k}^{k \le i+t}a_k) = \prod_{j+t < k}^{k \le i+t}\mathcal{A}(a_k) = \prod_{j < k}^{k \le i}\mathcal{A}(a_{k}) = \mathcal{A}(a_{i}^{j})$.
\end{proof}
\begin{proposition}
    If $\mathcal{A} \in \mathrm{O}_{t}(\mathbb{P}(a), \mathbb{R})$, then $\mathcal{A}(a_{i+j+t}^{i+j}) = \mathcal{A}(a_{i+t}^{i})$.
\end{proposition}
% \begin{proof}
%     \begin{align*}
%         \mathcal{A}(a_{i+j+t}^{i+j}) &= \prod_{i+j < k}^{k\le i+j+t}\mathcal{A}(a_{k+1}^{k}) \\
%         &= \prod_{i+j < k}^{k\le i+t}\mathcal{A}(a_{k+1}^{k}) \cdot \prod_{i+t < k}^{k\le i+j+t}\mathcal{A}(a_{k+1}^{k}) \\
%         &= \mathcal{A}(a_{i+t}^{i+j}) \cdot \prod_{i < k}^{k\le i+j}\mathcal{A}(a_{k+1}^{k}) \\
%         &= \mathcal{A}(a_{i+t}^{i+j})\cdot \mathcal{A}(a_{i+j}^{i}) \\
%         &= \mathcal{A}(a_{i+t}^{i}).
%     \end{align*}
% \end{proof}
\begin{proposition}~\label{prop:cycles-derive-cycle}
    Given $a\in\mathbb{G}$. If $\mathcal{A}, \mathcal{B} \in \mathrm{O}_{t}(\mathbb{P}(a), \mathbb{R})$, then $\forall \mathfrak{U} \in \partial_{t}(\mathbb{P}(a), \mathbb{R})$, we have $\mathfrak{U}(a_{i+t}^{j+t}) = \mathfrak{U}(a_{i}^{j})$.
\end{proposition}
% \begin{proof}
%     \begin{align*}
%         \mathfrak{U}(a_{i+t}^{j+t}) &= \sum_{j+t< k \le i+t}\mathcal{A}(a_{k}^{j+t})\cdot\mathfrak{U}(a_{k})\cdot\mathcal{B}(a_{i+t}^{k}) \\
%         &= \sum_{j+t< k \le i+t}\mathcal{A}(a_{k-t}^{j})\cdot\mathfrak{U}(a_{k-t})\cdot\mathcal{B}(a_{i}^{k-t}) \\
%         &= \sum_{j< k \le i}\mathcal{A}(a_{k}^{j})\cdot\mathfrak{U}(a_{k})\cdot\mathcal{B}(a_{i}^{k}) \\
%         &= \mathfrak{U}(a_{i}^{j}).
%     \end{align*}
% \end{proof}
\begin{theorem}~\label{thm:derivation-periodic-rule}
    Suppose $\mathcal{A}, \mathcal{B} \in \mathrm{O}_{t}(\mathbb{P}(a), \mathbb{R})$ and $\mathfrak{U}\in\partial_{t}(\mathbb{P}(a), \mathbb{R})$. Given $\{r_{i}\}\subset\mathbb{R}$, we have
    \[ r_i\cdot \left[\mathcal{A}(a_{i+t}^{i}) - \mathcal{B}(a_{i+t}^{i})\right] = \mathfrak{U}(a_{i+t}^{i}) \]
    holds for all $i\ge 0$ equivalents to for all these $i$, there is
    \[ \mathcal{A}(a_{i+1}^{i})\cdot r_{i+1} - r_i\cdot\mathcal{B}(a_{i+1}^{i}) = \mathfrak{U}(a_{i+1}^{i}). \]
    And these $r_i$ are also $t$-periodic.
\end{theorem}
\begin{proof}
    $\Longleftarrow$: similar with \cref{theorem:inductive-formula}. \newline
    $\Longrightarrow$: At first, consider $\mathfrak{U}(a_{i+t+1}^{i})$. Split it at $i+1$, we have
    \begin{align*}
        \mathfrak{U}(a_{i+t+1}^{i}) &= \mathcal{A}(a_{i+1}^{i})\mathfrak{U}(a_{i+t+1}^{i+1}) + \mathfrak{U}(a_{i+1}^{i})\mathcal{B}(a_{i+t+1}^{i+1}) \\
        &= \mathcal{A}(a_{i+1}^{i})\mathfrak{U}(a_{i+t+1}^{i+1}) + \mathfrak{U}(a_{i+1}^{i})\mathcal{B}(a_{i+t}^{i}).
    \end{align*}
    And then split it at $i+t$, we have
    \begin{align*}
        \mathfrak{U}(a_{i+t+1}^{i}) &= \mathcal{A}(a_{i+t}^{i})\mathfrak{U}(a_{i+t+1}^{i+t}) + \mathfrak{U}(a_{i+t}^{i})\mathcal{B}(a_{i+t+1}^{i+t}) \\
        &= \mathcal{A}(a_{i+t}^{i})\mathfrak{U}(a_{i+1}^{i}) + \mathfrak{U}(a_{i+t}^{i})\mathcal{B}(a_{i+1}^{i}).
    \end{align*}
    Thus we have
    \[ \mathfrak{U}(a_{i+1}^{i})\left[\mathcal{A}(a_{i+t}^{i}) - \mathcal{B}(a_{i+t}^{i})\right] = \mathcal{A}(a_{i+1}^{i})\mathfrak{U}(a_{i+t+1}^{i+1}) - \mathfrak{U}(a_{i+t}^{i})\mathcal{B}(a_{i+1}^{i}).\]
    Conpair with target equation, we have
    \begin{align*}
        &\mathfrak{U}(a_{i+1}^{i})\cdot\left[\mathcal{A}(a_{i+t}^{i}) - \mathcal{B}(a_{i+t}^{i})\right] \\
        =&\mathcal{A}(a_{i+1}^{i})\cdot r_{i+1}\cdot \left[\mathcal{A}(a_{i+1+t}^{i+1}) - \mathcal{B}(a_{i+1+t}^{i+1})\right] - r_i\cdot \left[\mathcal{A}(a_{i+t}^{i}) - \mathcal{B}(a_{i+t}^{i})\right]\cdot\mathcal{B}(a_{i+1}^{i})\\
        =&\mathcal{A}(a_{i+1}^{i})\cdot r_{i+1}\cdot \left[\mathcal{A}(a_{i+t}^{i}) - \mathcal{B}(a_{i+t}^{i})\right] - r_i\cdot \left[\mathcal{A}(a_{i+t}^{i}) - \mathcal{B}(a_{i+t}^{i})\right]\cdot\mathcal{B}(a_{i+1}^{i})\\
        =&\left[\mathcal{A}(a_{i+1}^{i})\cdot r_{i+1} - r_i\cdot\mathcal{B}(a_{i+1}^{i})\right]\cdot\left[\mathcal{A}(a_{i+t}^{i}) - \mathcal{B}(a_{i+t}^{i})\right].
    \end{align*}
    Thus just let $r_i$ sutisfy the iterative equation
    \[ \mathcal{A}(a_{i+1}^{i})\cdot r_{i+1} - r_i\cdot\mathcal{B}(a_{i+1}^{i}) = \mathfrak{U}(a_{i+1}^{i}), \]
    we have $r_i\cdot \left[\mathcal{A}(a_{i+t}^{i}) - \mathcal{B}(a_{i+t}^{i})\right] = \mathfrak{U}(a_{i+t}^{i})$. Besides, since $\mathcal{A}$, $\mathcal{B}$ and $\mathcal{U}$ are all $t$-periodic, the $r_{i}$ should be $t$-periodic.
\end{proof}
In \cref{exa:continous-situation-with-exact-differential}, we see that for continuous systems, if the given derivative kernel is an appropriate differential, the result is a difference of exponents with equal coefficients. For general systems, given a specific $\{r_{i}\}$, we also have this result. Therefore, we
\begin{definition}
    Say $\mathfrak{U}$ is an exact derivation of $\mathcal{A}$ and $\mathcal{B}$, when
    \[ \left.\left[\mathcal{A}(a_{i+t}^{i}) - \mathcal{B}(a_{i+t}^{i})\right]\ \right\vert \mathfrak{U}(a_{i+t}^{i}). \]
    And $\mathfrak{U}_{i+1}^{i}$ is called the $i$-step differential.
\end{definition}
\begin{definition}
    Given a ($\mathcal{A}$, $\mathcal{B}$)-derivation $\mathfrak{U} \in \partial_{*}(\mathcal{A}, \mathcal{B})$, if $\forall p, q\in \mathbb{P}(\mathbb{G})$, there is $\mathfrak{U}(p) = \mathfrak{U}(q)$, we say $\mathfrak{U}$ is kernel independent.
    We denote all kernel independent ($\mathcal{A}$, $\mathcal{B}$)-derivations as $\partial(\mathcal{A}, \mathcal{B})$.
\end{definition}
\begin{proposition}
    $\partial(\mathcal{A}, \mathcal{B}) = \partial_{1}(\mathcal{A}, \mathcal{B})$.
\end{proposition}
\begin{proof}
    For any partition $\mathbb{P}(a) = \{a_i\}_{i\in I}$. We have $\mathfrak{U}(a_{i+1}) = \mathfrak{U}(a_{i})$, thus $\mathfrak{U}$ is 1-periodic on $a$.
\end{proof}
Finially back to the Collatz complement. From \cref{proposition:collatz-complement-basic-prop2}, we can verify that, with arguments $(\alpha, \beta, \gamma)$, all Collatz complement $\mathfrak{A} \in \partial(\beta^{\mathcal{A}_{*}}, \alpha^{*})$, where the groupoid is $[\mathbb{Z}\times\mathbb{Z}]_{\rightsquigarrow}$ that $i \rightsquigarrow j$ as its element.
Inseparable elements of it is $\mathbb{P}([\mathbb{Z}\times\mathbb{Z}]_{\rightsquigarrow}) = \{i \rightsquigarrow (i + 1) \mid i \in \mathbb{Z}\}$. And the kernel $\mathfrak{A}(i \rightsquigarrow i + 1) = \gamma \cdot \mathfrak{A}_{i+1}^{i} = \gamma$.
Besides, the Collatz complement of Conway's extension of Collatz problem is actually those $\partial_{*}(\mathcal{A}, \mathcal{B})$, they are not kernel independent, but its kernels are $\beta$-periodic. Hence, no matter our Collatz complement, or Collatz complement of Conway's extension, just let $\mathcal{A}$ and $\mathcal{B}$ are $\beta$-periodic, they will all be periodic.
