% !TEX root = main.tex
\section{A Kind of Derivation on Groupoid}\label{sec:a-kind-of-derivation-on-groupoid}
In \cref{definition:collatz-complement} we defined the Collatz complement, they follow the proposition \cref{proposition:collatz-complement-basic-prop2}.
Now let's forget those definitions before.
We will introduce a new kind of form to subsume those properties above. It's the extend of $(\sigma, \tau)$-derivation on groupoids.

Let $\mathbb{G}(+, G)$ be a groupoid and $\mathbb{R}(+, \cdot, R)$ be a ring.
% Normally, we use lowercase letters to represent elements in $\mathbb{G}$ or $\mathbb{R}$. But for elements in $\mathbb{G}$, we introduce a notation consisting of two uppercase letters. They follow concatenation rules. That is, two uppercase letters can be added if and only if the first letter of the latter element is the same as the last letter of the former element. We also represent the inverse of an element by swapping letters. For example, $AC = AB + BC$ but $CD \ne CA + DE$, and $AB = -BA$.
With these, we can define groupoid homomorphisms and their derivtions from $\mathbb{G}$ to $\mathbb{R}$.
\begin{definition}[groupoid homomorphism]
    Function $\mathcal{A}: \mathbb{G} \to \mathbb{R}$ is called a groupoid homomorphism, if $\forall a, b \in \mathbb{G}$ that $a + b \in \mathbb{G}$, there is $\mathcal{A}(a+b) = \mathcal{A}(a) \cdot \mathcal{A}(b)$.
\end{definition}
\begin{proposition}
    If $\exists a \in \mathbb{G}$ that $\mathcal{A}(a) = 0$, then $\forall b \in \mathbb{G}$, we have $\mathcal{A}(b) = 0$.
    We called that $\mathcal{A}$ is a zero groupoid homomorphism, and denote $\mathcal{A} = 0$.
    Otherwise, we say $\mathcal{A} \ne 0$.
\end{proposition}
\begin{proposition}
    If $\mathcal{A} \ne 0$, then $\forall a \in \mathbb{G}$, there is $\mathcal{A}(a)\cdot\mathcal{A}(-a) = \mathcal{A}(0) = 1$. In other words, $\mathcal{A}(-a) = \mathcal{A}(a)^{-1}$. This requires the ring $\mathbb{R}$ at least is a division ring.
\end{proposition}
\begin{proposition}
    Denote $\mathrm{Hom}(\mathbb{G},\mathbb{R})$ as the set of all groupoid homomorphisms on $\mathbb{R}$, then $\mathrm{Hom}(\mathbb{G},\mathbb{R})$ is a group by
    \[ (\mathcal{A}\cdot\mathcal{B})(a) = \mathcal{A}(a)\cdot\mathcal{B}(a). \]
\end{proposition}
\begin{definition}[($\mathcal{A}$, $\mathcal{B}$)-derivation]~\label{def:quasi-derivation-on-groupoid}
    Given two groupoid homomorphisms $\mathcal{A}$ and $\mathcal{B}$.
    There's another function $\mathfrak{F}: \mathbb{G} \to \mathbb{R}$ such that $\forall a, b \in \mathbb{G}$, if $a + b \in \mathbb{G}$, then
    \[ \mathfrak{F}(a+b) = \mathcal{A}(a)\cdot\mathfrak{B}(b) + \mathfrak{B}(a)\cdot\mathcal{B}(b) \]
    called the forward ($\mathcal{A}$, $\mathcal{B}$)-derivation.
    And function $\mathfrak{B}: \mathbb{G} \to \mathbb{R}$
    \[ \mathfrak{B}(a+b) = \mathcal{A}(b)\cdot\mathfrak{B}(a) + \mathfrak{B}(b)\cdot\mathcal{B}(a) \]
    called the backward ($\mathcal{A}$, $\mathcal{B}$)-derivation.
\end{definition}
Actually, ($\mathcal{A}$, $\mathcal{B}$)-derivation is the extension of the $(\sigma, \tau)$-derivation on groupoid.
Given a ($\mathcal{A}$, $\mathcal{B}$)-derivation $\mathfrak{U}$, left or right both have
\begin{proposition}
    $\mathfrak{U}(0) = 0$.
\end{proposition}
\begin{proposition}
    $\mathcal{A}(a)\cdot\mathfrak{U}(-a) + \mathfrak{U}(a)\cdot\mathcal{B}(-a) = 0$.
\end{proposition}

Denote $\partial_{R}(\mathcal{A}, \mathcal{B})$ as the set of all right ($\mathcal{A}$, $\mathcal{B}$)-derivations and $\partial_{L}(\mathcal{A}, \mathcal{B})$ as the set of all left ($\mathcal{A}$, $\mathcal{B}$)-derivations.
If the left or right is not matter, i.e. $\mathbb{R}$ is commutative, we just denote them $\partial_{*}(\mathcal{A}, \mathcal{B})$.
Our discussion of ($\mathcal{A}$, $\mathcal{B}$)-derivations will base on $\mathbb{R}$ is commutative.
\begin{proposition}
    $\partial_{*}(\mathcal{A}, \mathcal{B})$ is a $\mathbb{R}$-module by
    \[ (s\mathfrak{U} + t\mathfrak{V})(a) = s\mathfrak{U}(a) + t\mathfrak{V}(a). \]
\end{proposition}
Also, we denote $\mathfrak{U} = 0$ if it is a zero ($\mathcal{A}$, $\mathcal{B}$)-derivation.
However, we need another restriction of ($\mathcal{A}$, $\mathcal{B}$)-derivations to describe our problem.

We say $p \in\mathbb{G}$ is inseparable, if $\forall a, b \in\mathbb{G}$, $p$ cannot be represented by $a + b$.
And we denote $\mathbb{P}(\mathbb{G}) = \{ AB \in\mathbb{G} \mid p \text{ is inseparable}\}$. With this we have
\begin{definition}[kernel of ($\mathcal{A}$, $\mathcal{B}$)-derivation]
    We say $\mathfrak{U}(p)$ is a kernel on $p$ of $\mathfrak{U}$ if $p \in \mathbb{P}(\mathbb{G})$. When $\mathbb{P}(\mathbb{G}) = \varnothing$, we define all kernels of $\mathfrak{U}$ are same.
\end{definition}
Now suppose there is an isomorphism in $\mathbb{G}^{I}$ such that $s \mapsto p(s)$ and $s + t \mapsto p(s) + p(t)$, here $I$ is some index set.
If $\exists t\in I$ such that $\forall p(s)\in \mathbb{P}(\mathbb{G})$, such that $F:\mathbb{G} \to \mathbb{R}$ satisfied $F(p(s + t)) = F(p(s))$, then we say $F$ is periodic on $\mathbb{G}^{I}$ with $t$ is one period of it.
\begin{definition}
    All periodic groupoid homomorphism with period $t$ denote as $\mathrm{Hom}_{\circ(t)}(\mathbb{G},\mathbb{R})$.
    And all periodic kernel ($\mathcal{A}$, $\mathcal{B}$)-derivations with period $t$ denote as $\partial_{\circ(t)}(\mathcal{A}, \mathcal{B})$.
\end{definition}
\begin{proposition}
    If $\mathcal{A}, \mathcal{B} \in \mathrm{Hom}_{\circ(t)}(\mathbb{G}, \mathbb{R})$, then $\forall \mathfrak{U} \in \partial_{\circ(t)}(\mathcal{A}, \mathcal{B})$, $\forall a \in\mathbb{G}$, we have
    \[ \mathfrak{U}(a(s + t)) = \mathfrak{U}(a(s)) \]
\end{proposition}
\begin{definition}
    Given a ($\mathcal{A}$, $\mathcal{B}$)-derivation $\mathfrak{U} \in \partial_{*}(\mathcal{A}, \mathcal{B})$, if $\forall p, q\in \mathbb{P}(\mathbb{G})$, there is $\mathfrak{U}(p) = \mathfrak{U}(q)$, we say $\mathfrak{U}$ is kernel independent.
    All kernel independent ($\mathcal{A}$, $\mathcal{B}$)-derivations are denoted as $\partial(\mathcal{A}, \mathcal{B})$.
\end{definition}
\begin{proposition}
    $\partial(\mathcal{A}, \mathcal{B}) = \partial_{\circ(1)}(\mathcal{A}, \mathcal{B})$.
\end{proposition}

We say a sequence $\mathbb{T}(a)$ on $\mathbb{G}$ is a partition of $a \in \mathbb{G}$, if $\forall c \in \mathbb{T}(a)$, $\exists b, d \in \mathbb{G}$, such that $a = b + c + d$. And $\sum_{c\in \mathbb{T}(a)}c = a$. We denote $b = *c$ and $d = c*$. If $\forall c \in \mathbb{T}(a)$, there is $c\in\mathbb{P}(\mathbb{G})$, then we also denote the partition as $\mathbb{P}(a)$.
\begin{theorem}
    $\forall \mathfrak{U} \in \partial_{*}(\mathcal{A}, \mathcal{B})$ we have
    \[ \mathfrak{U}(a) = \sum_{c \in \mathbb{T}(a)}\mathcal{A}(*c)\cdot\mathfrak{U}(c)\cdot\mathcal{B}(c*). \]
\end{theorem}
Specially, For the kernel independent ($\mathcal{A}$, $\mathcal{B}$)-derivation, we have
\begin{corollary}
    If $\mathfrak{U} \in \partial(\mathcal{A}, \mathcal{B})$ that $\mathfrak{U}(p) = \gamma$, then
    \[ \mathfrak{U}(c) = \gamma \cdot\sum_{p \in \mathbb{P}(a)}\mathcal{A}(*p)\cdot\mathcal{B}(p*). \]
\end{corollary}
This corollary also holds on those continuous system.
\begin{definition}[varied homomorphism derivation and their varities]~\label{def:varied-homomorphism derivation-and-their-varities}
\begin{align*}
    \mathcal{C}\mathfrak{U} &= \sum_{c \in \mathbb{T}(a)}(\mathcal{C}\cdot\mathcal{A})(*c)\cdot\mathfrak{U}(c)\cdot\mathcal{B}(c*),\\
    \mathfrak{U}\mathcal{C} &= \sum_{c \in \mathbb{T}(a)}\mathcal{A}(*c)\cdot\mathfrak{U}(c)\cdot(\mathcal{B}\cdot\mathcal{C})(c*),\\
    \delta_{\sigma}\mathfrak{U} &= \mathcal{C}\mathfrak{V} - \mathcal{C}(a)\cdot\mathfrak{U} = \sum_{c \in \mathbb{T}(a)}\left[1 - \mathcal{C}(c + c*)\right]\cdot(\mathcal{C}\cdot\mathcal{A})(*c)\cdot\mathfrak{U}(c)\cdot\mathcal{B}(c*),\\
    \delta_{\tau}\mathfrak{U} &= \mathfrak{V}\mathcal{C} - \mathfrak{U}\cdot \mathcal{C}(a) = \sum_{c \in \mathbb{T}(a)}\mathcal{A}(*c)\cdot\mathfrak{U}(c)\cdot(\mathcal{B}\cdot\mathcal{C})(c*)\cdot\left[1 - \mathcal{C}(*c + c)\right].
\end{align*}
\end{definition}
Finially back to the Collatz complement. With arguments $(\alpha, \beta, \gamma)$, all Collatz complement $\mathfrak{A} \in \partial(\beta^{\mathcal{A}_{*}}, \alpha^{*})$, where the groupoid is $[\mathbb{Z}\times\mathbb{Z}]_{\rightsquigarrow}$ that $i \rightsquigarrow j$ as its element.
Inseparable elements of it is $\mathbb{P}([\mathbb{Z}\times\mathbb{Z}]_{\rightsquigarrow}) = \{i \rightsquigarrow (i + 1) \mid i \in \mathbb{Z}\}$. And the kernel $\mathfrak{A}(i \rightsquigarrow i + 1) = \gamma \cdot \mathfrak{A}_{i+1}^{i} = \gamma$.
Besides, the Collatz complement of Conway's extension of Collatz problem is actually those $\partial_{*}(\mathcal{A}, \mathcal{B})$, they are not kernel independent, but its kernels are $\beta$-periodic. Hence, no matter our Collatz complement, or Collatz complement of Conway's extension, just let $\mathcal{A}$ and $\mathcal{B}$ are $\beta$-periodic, they will all be periodic.
